功能：
	对章节名称文字加粗显示
	对页眉页脚使用特别显示（字体，字号，颜色等等）
	可以调整显示字号，字本
	可以直接通过http下载阅读
	多语言界面支持
	添加功能图标
	显示风格（章节名称，页眉，页脚等）可以通过定制
	内容中识别关键字链接（匹配RFC+数字）
	相关RFC（根据本文内容关键字匹配，加index.txt识别）
BUG：
	修正导航定位的正文抖动问题
	如何处理'\f'?保留以支持打印（支持类似word的分页显示？）？还是不保留。。。（目前是读取时使用了'\f'来识别分页，之后未保留）